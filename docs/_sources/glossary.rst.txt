Glossary
========

.. glossary::

    root directory
      The top level directory which must contain a :file:`pyAutoMark.json` configuration file.

    cohort
      Represents a set of students doing a particular assignment. Each cohort will have a separate directory
      for the students submissions, for the tests and for the reports.

